jmp main

putcstr:
    putc
    dup 1
    cast 1
    cjmp putcstr
    ret

# this is a comment
; so is this!
// and this :D

main:
    push '\0 
    # this compiles, but i'm too lazy to report an error
    push '\u000a'
    push 'a'
    push 'b'
    push 'c'
    push 'A'
    push 'B'
    push 'C' 

    call putcstr

hlt
